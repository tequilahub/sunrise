This is an illustrative example on how to build hybridized orbitals in a sunrise molecule.
It is not part of the `sunrise` package, so run the example as a module from the `miscellaneous` folder:

    python -m hybridization.water_example
